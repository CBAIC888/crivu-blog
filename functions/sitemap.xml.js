import { articles, bundle, collections, projects } from './_content.js';
export async function onRequest({env,request}){const {db}=await bundle(env),origin=new URL(request.url).origin;const [a,c,p]=await Promise.all([articles(db),collections(db),projects(db)]);const paths=['/articles','/issues','/about',...a.map(x=>`/articles/${encodeURIComponent(x.slug)}`),...c.map(x=>`/issues/${encodeURIComponent(x.id)}`),...p.map(x=>`/records/${encodeURIComponent(x.slug)}`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(path=>`<url><loc>${origin}${path}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8','Cache-Control':'public, max-age=3600'}});}
